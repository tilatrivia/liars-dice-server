{
  "name": "liars-dice-server",
  "version": "1.0.0",
  "description": "Server for handling clients playing liars dice",
  "main": "dist/main.js",
  "scripts": {
    "server": "npm run build && node dist/main.js",
    "build": "npm run build:ts",
    "build:ts": "rm -r dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tilatrivia/liars-dice-client.git"
  },
  "author": "Truman Kautzman",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/tilatrivia/liars-dice-client/issues"
  },
  "homepage": "https://github.com/tilatrivia/liars-dice-client#readme",
  "dependencies": {
    "socket.io": "^4.7.1"
  },
  "devDependencies": {
    "typescript": "^5.1.6"
  }
}
